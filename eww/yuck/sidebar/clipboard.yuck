;(defpoll curr_clips :interval "5s" "scripts/curr-clips")

;(defwidget clipboard []
;    (box
;        :class "clipboard-container"
;        (label
;            :text curr_clips
;            :limit-width 15
;        )
;    )
;    )
